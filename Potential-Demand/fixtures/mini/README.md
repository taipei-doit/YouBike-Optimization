# Mini fixtures

Generated by `make fixtures` / `python scripts/generate_mini_fixtures.py`.

Used only for `make smoke` (bridge → Docker clj-etl → parity).  
Does **not** replace private real data under `input/data/`.
